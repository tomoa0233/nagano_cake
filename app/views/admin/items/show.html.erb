<h5>商品詳細</h5>
<%= attachment_image_tag @item, :image, format: 'jpeg', size: '300x300' %>
<p>商品名<%= @item.name %></p>
<p>商品説明<%= @item.introduction %></p>
<p>ジャンル<%= @item.genre.name %></p>
<p>税込価格(税抜価格)<%= @item.with_tax_price %>(<%= @item.price %>) </p>
<p>販売ステータス
 <% if @item.is_active == true %>
  <%= "販売中" %>
 <% else %>
  <%= "販売停止中" %>
 <% end %></p>
<%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-sm btn-success" %>