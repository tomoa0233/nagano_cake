<div class="container">
 <div class="row">
  <h5>ジャンル編集</h5>
 </div>
 <div class="col-lg-6">
  <table class="table table-borderless">
   <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
   <tbody>
   <tr>
    <td class="col-lg-2"><%= f.label :genre, "ジャンル名" %></td>
    <td class="col-lg-2"><%= f.text_field :name, autofocus: true, placeholder:"ジャンル名" %></td>
    <td class="col-lg-2"><%= f.submit '変更を保存', class: "btn btn-sm btn-success" %></td>
   </tr>
   <% end %>
   </tbody>
   </table>
   </div>
   </div>
   </div>